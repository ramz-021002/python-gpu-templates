numpy>=1.26.0

# Hugging Face + TensorFlow compatibility set.
# Transformers v5 removed TensorFlow auto-model APIs, so keep <5
# if you want TFAutoModel / TF inference through transformers.
transformers>=4.41,<5
huggingface-hub>=0.23,<1.0

# TensorFlow range used by this template on Linux/Windows CUDA.
# macOS Apple Silicon should use tensorflow-macos + tensorflow-metal.
tensorflow>=2.16,<2.21; platform_system != "Darwin"
tf-keras>=2.16,<2.21; platform_system != "Darwin"
